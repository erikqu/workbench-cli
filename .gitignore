node_modules/
artifacts/
.vendor/
.DS_Store
*.log

# Personal site repo is sometimes checked out here to update /install; it is a
# separate repo and must never be committed into this one.
erikqu.github.io/

# Local scratch space: throwaway diagnostic scripts, never committed.
scratch/
